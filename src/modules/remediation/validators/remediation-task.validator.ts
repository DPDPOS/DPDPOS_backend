// Input validation for remediation tasks is schema-first via the zod DTOs in
// ../dto/ (create/update/close/list). This file exists to keep the module's
// layer layout consistent with the rest of the platform.
